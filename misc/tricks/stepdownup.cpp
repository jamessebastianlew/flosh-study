#include <bits/stdc++.h>

using namespace std;

int n;
int vals[1000];

int main() {

    // creating an array of 1 and -1 depending on whether the number is odd
    // or even, can be done with any condition, just replace a&1
    //
    // odd = 1
    // even = -1

    for(int i=0; i<n; ++i)
        cin >> temp, vals[i] = 2*(a&1)-1;

    return 0;
}


